# HOW TO EDIT THIS FILE:
# The "handy ruler" below makes it easier to edit a package description.
# Line up the first '|' above the ':' following the base package name, and
# the '|' on the right side marks the last column you can put a character in.
# You must make exactly 11 lines for the formatting to be correct.  It's also
# customary to leave one space after the ':' except on otherwise blank lines.

       |-----handy-ruler------------------------------------------------------|
grub-config: grub-config (short description of app)
grub-config:
grub-config: Long description of grub-config, wrapped at 71 characters *after* the
grub-config: colon following "grub-config" (and the 'handy ruler' should start at
grub-config: that colon and be exactly 71 characters long).
grub-config: If there is room, there might be a link to the homepage of the
grub-config: application on one of these lines, but it's not necessary.
grub-config:
grub-config: The maximum number of lines prefixed by "grub-config:" is 11.
grub-config: Lines without any other text should *not* have a space after the :
grub-config:
