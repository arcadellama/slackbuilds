#!/bin/sh
# Start/stop the NZBget daemon.
#
USER=nzbget
PREFIX=/usr/bin

case "$1" in
start)   echo -n "Start services: NZBget"
$PREFIX/nzbget -o DaemonUsername=$USER -D
;;
stop)   echo -n "Stop services: NZBget"
$PREFIX/nzbget -Q
;;
*)   echo "Usage: $0 start|stop"
exit 1
;;
esac
exit 0
